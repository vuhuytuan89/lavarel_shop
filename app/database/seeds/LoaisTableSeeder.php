<?php

class LoaisTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('loais')->truncate();

		$loais = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('loais')->insert($loais);
	}

}
