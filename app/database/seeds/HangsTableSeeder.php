<?php

class HangsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('hangs')->truncate();

		$hangs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('hangs')->insert($hangs);
	}

}
